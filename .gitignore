# Rust
target/
**/*.rs.bk
# Workspace policy: lib crate, do NOT commit lock file.
Cargo.lock

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp

# Tooling
*.profraw
flamegraph.svg
perf.data*

# CCD / Claude Code session metadata (worktrees, etc.)
.claude/

# RepoLens scan output (derived data: packs, session state, wiki index)
.repolens/

# v1.12 scope — length-committed streaming SM4-CCM (`Sm4CcmEncryptor` / `Sm4CcmDecryptor`)

**Status:** scope proposed; forks **Q12.1–Q12.12 await maintainer sign-off**
before any code lands. Q12.1 and Q12.10 were settled in the scoping
conversation on 2026-09-01 and are recorded here with their alternatives so a
different choice stays cheap.

**Cycle type:** publishing. Workspace `1.11.2` → **`1.12.0`** (lockstep; sibling
pins `=1.12.0`). Publish order simd → core → c, the maintainer's authenticated
per-release call. `gmcrypto-c` and `gmcrypto-simd` carry no source change this
cycle — they bump because the workspace bumps.

**Numbering note.** `Q12.x` collides with `docs/v0.12-scope.md`'s `Q12.x` (the
SM4-XTS cycle). That is the house style; references are always doc-qualified
("`docs/v1.12-scope.md` Q12.3").

---

## 1. Why now

"SM4-CCM buffered input" has sat on the deferred list since v0.9 (Q9.1), and
the v0.15 cycle (Q15.11) recorded the reason it stayed there:

> An "incremental-input" CCM API could only *buffer* the whole message and
> process it at finalize — **zero** memory benefit over the single-shot, a
> misleading "streaming" name, and a permanent SemVer surface for a
> non-feature.

That verdict is correct for the design it evaluated — one that lets the caller
feed an unknown-length plaintext. It did not evaluate the **length-committed**
design, in which the caller declares the plaintext length at construction. CCM
encodes that length in the first CBC-MAC block `B0` (RFC 3610 §2.2); once `B0`
is known the CBC-MAC and the CTR keystream both advance chunk by chunk with
`O(chunk)` memory and no buffering at all. This is the contract OpenSSL's EVP
SM4-CCM already imposes on C callers (`EVP_EncryptUpdate` with a NULL output to
set the total length before any data), and it fits the known-length profiles
the `mode_ccm` module documentation itself names — 802.15.4 / Zigbee frames and
TLS-CCM records, where the length is in the header before the payload arrives.

So the feature that was a non-feature under one design is a genuine one under
the other: a real streaming *encryptor* with the memory story the v0.15
objection demanded, plus a decryptor that exists for API parity with
[`Sm4GcmDecryptor`] and is honestly labelled as what it is.

**What this cycle is not.** No new cryptography and no new dependency. The
encryptor is the single-shot `mode_ccm::encrypt` algorithm re-ordered around
chunk boundaries; the decryptor is a thin wrapper over the single-shot
`mode_ccm::decrypt`. Both live behind the existing `sm4-aead` feature. The
default-features build stays byte-identical, and the C ABI is untouched
(Q12.10).

## 2. The structural fact the design rests on

`mode_ccm::encrypt` today:

1. `validate_params(nonce, pt_len, aad_len, tag_len)`;
2. `auth = B0 ‖ format(aad) ‖ plaintext ‖ zero-pad`, then `T = CBC-MAC(auth)`;
3. `S0 = E(A0)`; `tag = (S0 ⊕ T)[..tag_len]`;
4. `ct = plaintext ⊕ CTR(A1, A2, …)`.

Every quantity in steps 1–3 except the plaintext bytes themselves is known at
construction once `pt_len` is declared. The CBC-MAC is a chain — it absorbs
`B0 ‖ format(aad)` first and the plaintext blocks after, so the prefix can be
folded at construction and plaintext absorbed as it arrives. The CTR keystream
is indexed by block number, so it can be generated as needed with a leftover
buffer for partial-block chunks (the exact idiom `Sm4GcmEncryptor` uses).
Nothing about CCM prevents streaming *encryption*; it only prevents streaming
without a length commitment.

Decryption is different for the reason every AEAD decryptor is: releasing
plaintext before the tag verifies hands an attacker a chosen-ciphertext
distinguisher, so the output is buffered until `finalize_verify` regardless of
mode. CCM adds that the MAC runs over the *plaintext*, so no MAC work can begin
until the length is known — which for a stream is EOF. A decryptor therefore
buffers ciphertext and does all its work at finalize; that is the GCM
decryptor's memory shape (`O(message)`) with less spread-out compute, and it is
documented as such.

## 3. Forks for sign-off

### Q12.1 — Scope: encryptor and decryptor, not decryptor alone
**Settled 2026-09-01: both.** The encryptor is the component with the memory
story; the decryptor is API parity with the GCM pair and the natural consumer
of the encryptor's output. **Alternative (rejected):** decryptor only — the
smallest surface, but exactly the buffer-only shape Q15.11 called a
non-feature. **Alternative (rejected):** re-affirm the deferral — no consumer
has surfaced since v0.9, but the deferral rested on a design premise this cycle
overturns, and the surface is small.

### Q12.2 — Placement: a `ccm_streaming` sibling module under `sm4-aead`
**Recommend:** new file `crates/gmcrypto-core/src/sm4/ccm_streaming.rs`,
declared `#[cfg(feature = "sm4-aead")] pub mod ccm_streaming;` and re-exported
as `pub use ccm_streaming::{Sm4CcmDecryptor, Sm4CcmEncryptor};` — the exact
shape of `gcm_streaming` (`sm4/mod.rs:50` and `:90`). No new feature flag: the
types are meaningless without `mode_ccm`, and a new flag would pay the
seven-places tax (`Cargo.toml`, `deny.toml`, `ci.yml`, `api-stability.yml`,
clippy leg, MSRV leg, wasm32 leg) for nothing.
**Alternative:** implement inside `mode_ccm.rs`. Keeps the helpers private but
takes the file past 1,100 lines and breaks the streaming-lives-beside-the-mode
precedent.

### Q12.3 — Encryptor API and the length-commit contract
**Recommend:**

```rust
pub struct Sm4CcmEncryptor { /* private */ }
impl Sm4CcmEncryptor {
    #[must_use]
    pub fn new(key: &[u8; KEY_SIZE], nonce: &[u8], aad: &[u8],
               plaintext_len: usize, tag_len: usize) -> Option<Self>;
    #[must_use]
    pub fn update(&mut self, chunk: &[u8]) -> Option<Vec<u8>>;
    #[must_use]
    pub fn finalize(self) -> Option<Vec<u8>>;
}
```

- `new` runs the same `validate_params` gate as the single-shot (nonce length
  in `[7, 13]`, `tag_len` in `{4, 6, 8, 10, 12, 14, 16}`, `plaintext_len`
  representable in `q = 15 − nonce.len()` bytes) and returns `None` on any
  failure. Unlike `Sm4GcmEncryptor::new`, this constructor is fallible —
  CCM has parameters GCM does not.
- `update` returns the ciphertext for `chunk` (same length; CCM neither pads
  nor buffers output). If `fed + chunk.len()` would exceed `plaintext_len`
  the call returns `None` **and poisons the encryptor** — every later call
  returns `None`. The already-emitted ciphertext is not retracted (it cannot
  be) but no tag will ever authenticate it.
- `finalize` returns the `tag_len`-byte tag, or `None` if the encryptor is
  poisoned **or under-fed** (`fed != plaintext_len`). A partial stream is never
  tag-authenticated: the length in `B0` is a commitment, and an under-fed
  finalize would produce a tag over a message the caller did not send.
- The tag is returned separately, not appended, matching `Sm4GcmEncryptor` and
  the wire layout the decryptor consumes (Q12.4). Callers wanting the
  single-shot's `ct ‖ tag` concatenate.

**Alternative:** `finalize` returning `Vec<u8>` unconditionally with the
under-feed case producing a tag over the shorter message. Rejected: it turns a
caller bug into a validly-tagged message with the wrong declared length and
recreates the class of truncation confusion CCM's `B0` exists to prevent.

### Q12.4 — Decryptor API: length-free, commit-on-verify, thin
**Recommend:**

```rust
pub struct Sm4CcmDecryptor { /* private */ }
impl Sm4CcmDecryptor {
    #[must_use]
    pub fn new(key: &[u8; KEY_SIZE], nonce: &[u8], aad: &[u8]) -> Option<Self>;
    pub fn update(&mut self, chunk: &[u8]);
    #[must_use]
    pub fn finalize_verify(self, tag: &[u8]) -> Option<Vec<u8>>;
}
```

- No length parameter: a decryptor frequently does not know the payload length
  until EOF, and requiring it would make the type strictly harder to use than
  the GCM decryptor for no memory gain (the output must be buffered until
  verification either way).
- `new` validates the nonce length early (`None` outside `[7, 13]`) so a
  misconfigured decryptor fails before it buffers a message. `tag_len` is not
  known until `finalize_verify` and is taken from `tag.len()`, exactly as
  `Sm4GcmDecryptor::finalize_verify` does; an invalid length is `None`.
- `update` appends to an internal ciphertext buffer and emits nothing. No
  overflow latch is needed: `validate_params` rejects an over-long payload at
  finalize with the same single `None`, and the buffer is bounded by the
  caller's memory as the GCM decryptor's is.
- `finalize_verify` runs the single-shot decrypt body over `buffer ‖ tag`
  (Q12.5) and returns the plaintext only on tag verification. On failure the
  tentative plaintext is zeroized by that body — it is today's code.

**Why thin matters (the assurance premise, as in v1.11 Q11.8):** because the
decryptor adds no cryptography over `mode_ccm::decrypt`, it rides the existing
`ct_sm4_ccm_decrypt` dudect target (Q12.8), and that thinness is guarded by
the differential fuzz target (Q12.9) rather than by this paragraph.

**Alternative:** a length-declared decryptor mirroring the encryptor, doing
CTR + CBC-MAC per chunk and buffering plaintext. Spreads compute, but keeps an
unverified plaintext buffer alive across calls (a larger zeroize surface),
forces the caller to know the length before EOF, and would be new
secret-touching code needing its own dudect target. Rejected.

### Q12.5 — `mode_ccm` refactor: promote helpers, split `decrypt`
**Recommend:** promote `validate_params`, `build_b0`, and `format_aad_into`
from private to `pub(crate)` (the `mode_gcm` precedent: `derive_j0`, `gctr`,
`inc32`, `GCM_MAX_PT_BYTES` are `pub(crate)` for `gcm_streaming`), and factor
the body of `decrypt` into
`pub(crate) fn decrypt_with_cipher(cipher: &Sm4Cipher, nonce, aad, ciphertext_with_tag, tag_len) -> Option<Vec<u8>>`
with the public `decrypt` becoming `Sm4Cipher::new(key)` followed by that
call. The public single-shot API, its outputs, and its KATs do not change; the
decryptor gains a stored `Sm4Cipher` instead of key bytes and does not re-run
the key schedule at finalize. No item becomes `pub` — nothing new enters the
SemVer surface from this refactor.
**Alternative:** duplicate the three helpers in `ccm_streaming.rs`. Rejected:
two copies of `B0` formatting is exactly the drift the differential tests would
then be needed to catch.

### Q12.6 — Zeroize
**Recommend:** `Sm4CcmEncryptor` derives `Zeroize` + `ZeroizeOnDrop`. Its state
carries plaintext-derived bytes across calls — the CBC-MAC partial block (raw
plaintext), the running `T`, and the leftover keystream — and `Sm4Cipher`
already zeroizes on drop, so this is the crate's established best-effort
posture applied consistently. `Sm4CcmDecryptor` gets no zeroize: its buffer is
ciphertext (public), the AAD is public, and no plaintext exists until
`finalize_verify` succeeds (the failure path inside `decrypt_with_cipher`
zeroizes the tentative plaintext already). The usual caveat applies and is
restated in the module docs: stack copies and register spills are outside what
`zeroize` can reach.
**Note for review:** `Sm4GcmEncryptor` does **not** currently zeroize its
leftover keystream. Bringing it into line is a one-line follow-up outside this
cycle's diff; it is recorded, not folded in, to keep this PR's scope honest.

### Q12.7 — Failure modes
Every fallible operation returns `Option`; `None` covers, without distinction:
invalid nonce length; invalid tag length; `plaintext_len` too large for `q`;
over-feed (encryptor, with poison); under-feed at `finalize`; buffered
ciphertext too long for `q` at `finalize_verify`; tag mismatch. This is the
workspace failure-mode invariant (`SECURITY.md`); a PR that adds a
distinguishing variant is rejected on that basis.

### Q12.8 — Dudect: no new target (count stays 20)
**Recommend: none**, with the rationale recorded in `SECURITY.md` beside the
v1.11 paragraph:

- The **encryptor's** secret-dependent work is the same `Sm4Cipher::encrypt_block`
  chain the single-shot runs (CBC-MAC over plaintext, CTR keystream). Its only
  new control flow is the chunk boundary, which is a public quantity chosen
  by the caller. `Sm4GcmEncryptor` has no target on the same reasoning —
  encryption has no tag comparison and no failure arm — and `ct_sm4_ctr_encrypt`
  already gates the keystream path class-split by key.
- The **decryptor** is a thin wrapper over the single-shot body already
  measured by `ct_sm4_ccm_decrypt`; the code that touches the key is
  byte-identical, and the differential fuzz target fails the sweep if a future
  rewrite quietly reimplements mode logic in the wrapper.

**Alternative:** a `ct_sm4_ccm_decrypt_buffered` target mirroring
`ct_sm4_gcm_decrypt_buffered`. Rejected: that GCM target exists because the GCM
decryptor carries a genuinely new secret-touching body (the running GHASH
accumulator). The CCM decryptor carries none; a target would measure the same
window twice and add a slot to a matrix whose noise budget (see
`docs/v0.5-dudect-recalibration.md`, 2026-09-01) is already the cycle's most
expensive resource.

### Q12.9 — Fuzz: two differential targets (census 33 → 35)
**Recommend:**

- `fuzz_sm4_ccm_streaming_decrypt` — the `fuzz_sm4_gcm_streaming_decrypt`
  template: arbitrary key / nonce / AAD / chunk size / ciphertext, a wire tag
  drawn from the input, fed to `Sm4CcmDecryptor` in fixed chunks and asserted
  byte-equal (`Some == Some` plaintext or `None == None`) to
  `mode_ccm::decrypt` fed all at once. Tag length is drawn from the seven
  valid values *and* invalid ones so the `None == None` arm is exercised; a
  `None` from `Sm4CcmDecryptor::new` (invalid nonce length) counts as the
  streaming side's `None` and must coincide with the single-shot's.
- `fuzz_sm4_ccm_streaming_encrypt` — the `fuzz_sm4_gcm_encrypt` template:
  whenever the single-shot accepts the parameters, the streaming
  `ciphertext ‖ tag` across an arbitrary chunking byte-equals
  `mode_ccm::encrypt`, and `mode_ccm::decrypt` round-trips it. Also drives
  the over-feed and under-feed arms and asserts `None`.

Both take SM4, so both go in `FUZZ_TARGETS` **and** `FUZZ_SIMD_TARGETS`; each
gets a committed `fuzz/seeds/<target>/` directory (the #117 preflight fails
the nightly otherwise). Seed layouts follow the `arbitrary` 1.4.2
front-consuming order pinned by `fuzz/Cargo.lock`.

### Q12.10 — FFI: deferred to a follow-up cycle
**Settled 2026-09-01: defer.** The v0.9 → v0.10 precedent — Rust streaming
semantics ship first, the permanent C ABI follows once they have settled.
The `gmcrypto_sm4_ccm_encryptor_*` / `_decryptor_*` handle family (lifecycle
identical to the GCM streaming handles) is recorded as the **v1.13 candidate**.
No `aead-traits` change either: `aead` 0.6 has no `aead::stream`, so the
streaming types stay inherent-only exactly as the GCM pair does.

### Q12.11 — Terminology
**Recommend:** the module and type docs use two fixed phrases and never the
bare word "streaming" for the decryptor: the encryptor is
**"length-committed streaming"**; the decryptor is **"incremental-input
buffered"** (the GCM term). The v0.15 objection (Q15.11) is cited in the module
docstring and answered there, so a future reader sees why the name changed
rather than assuming the objection was forgotten.

### Q12.12 — Release mechanics
- **Version:** `1.12.0` in the release-prep PR, not the feature PR (all three
  crates, lockstep; `=1.12.0` sibling pins).
- **`docs/api-baseline/gmcrypto-core.txt`** is regenerated **in the feature
  PR** with the pinned tool — the api-stability drift-check is not in the local
  gate and fails the PR otherwise.
- **`CLAUDE.md`:** the "v1.11 — the current release" subsection is *replaced*
  by a v1.12 one and the v1.11 text is prepended to `docs/version-history.md`;
  the release table's workspace-version row moves to `1.12.0` prepped.
- **`CHANGELOG.md`** Unreleased → `[1.12.0]`; **`SECURITY.md`** gains the Q12.8
  paragraph and the fuzz census update; the module docs carry a doctest on `?`.
- Publish remains the maintainer's authenticated call; the agent path ends at
  the release-prep PR with the `cd`-inside-the-block publish commands and a
  tag command naming the SHA.

## 4. Assurance plan

| Layer | What proves it |
|---|---|
| Unit — encryptor | Chunked-equals-single-shot over chunk sizes `{1, 7, 15, 16, 17, 31, 32, 33, 100, whole}` for a 200-byte payload; all seven tag lengths; all nonce lengths 7–13; empty AAD; `plaintext_len = 0`; over-feed poisons (and stays poisoned); under-feed `finalize` → `None`; `new` rejects each invalid parameter |
| Unit — decryptor | Same chunk matrix against `mode_ccm::decrypt`; tag mismatch → `None`; invalid `tag.len()` → `None`; invalid nonce → `new` is `None`; round-trip from the streaming encryptor |
| KAT | The existing OpenSSL-sourced CCM vectors (`docs/v0.8-ccm-kat-sourcing.md`) driven through both streaming types in ≥2 chunkings |
| Single-shot regression | Existing `mode_ccm` tests and KATs unchanged after the Q12.5 refactor |
| Fuzz | Q12.9 targets; initial local sweep before the PR; nightly thereafter |
| Dudect | No new target (Q12.8); the existing `ct_sm4_ccm_decrypt` continues to gate the shared body on the 4th matrix leg |
| CI legs | `cargo clippy -p gmcrypto-core --features sm4-aead --all-targets -- -D warnings`; `cargo test -p gmcrypto-core --features sm4-aead`; the `sm4-aead,sm4-bitsliced-simd` wasm32 build; MSRV 1.85 build; `cargo doc` with `-D warnings` run **locally** (the intra-doc-link gate is CI-only) |
| Boundary | `check_disclosure_boundary.py --worktree`; `cargo fmt --all -- --check` plus the fuzz workspace's own `cargo fmt --manifest-path fuzz/Cargo.toml --all` |

## 5. Risks

- **The Q12.5 refactor perturbing the single-shot.** Mitigation: the public
  `decrypt` becomes a two-line delegator; the existing KATs and
  `fuzz_sm4_ccm_decrypt` / `fuzz_sm4_ccm_encrypt` sweeps are unchanged and gate
  it.
- **Partial-block bookkeeping bugs** in the CBC-MAC accumulator or CTR
  leftover. Mitigation: chunk size 1 and every block-boundary neighbour are in
  the unit matrix; the fuzz targets draw chunk sizes freely.
- **Counter width.** `A_i`'s counter is `q` bytes; `fed_len ≤ plaintext_len ≤
  2^(8q) − 1` is enforced at `new`, so the block index never exceeds the field.
  Stated in a code comment at the counter encode, not left to the reader.
- **Nonce reuse across encryptor instances** is the caller's contract, as for
  every mode here; restated in the type docs with the CCM-specific note that a
  `(key, nonce)` pair also fixes `B0`, so two encryptors with the same pair and
  different `plaintext_len` are still a nonce reuse.
- **`O(message)` decryptor memory** could surprise a caller who read
  "streaming" on the encryptor. Mitigation: Q12.11 — the decryptor is never
  called streaming.

## 6. Out of scope

FFI projection (v1.13 candidate, Q12.10); `aead-traits` integration (no
`aead::stream`); a length-declared decryptor (Q12.4 alternative); bringing
`Sm4GcmEncryptor`'s leftover keystream under zeroize (Q12.6 note); any change to
`mode_ccm`'s public single-shot API; AVX-512 `sbox_x64` and F21, which stay on
the deferred list as before.

## 7. Sequence

1. **Spec PR** — this document (no code).
2. **Implementation PR** — `ccm_streaming.rs`, the Q12.5 refactor, unit tests
   and KAT plumbing, the two fuzz targets with seeds and workflow-list entries,
   module docs + doctest, `SECURITY.md` paragraph, `CHANGELOG.md` Unreleased,
   regenerated api-baseline. Full local gate (§4) before opening; PR-triggered
   dudect smoke runs on the `sm4-aead` legs because `crates/gmcrypto-core/src/**`
   is in the trigger allowlist.
3. **Release-prep PR** — version `1.12.0`, CHANGELOG heading, CLAUDE.md
   subsection replace + version-history prepend, publish/tag commands for the
   maintainer.
4. **Publish** — maintainer.

[`Sm4GcmDecryptor`]: https://docs.rs/gmcrypto-core/latest/gmcrypto_core/sm4/struct.Sm4GcmDecryptor.html

# v1.11.1 — ECOSYSTEM §8 Gate #1 evidence

`docs/ECOSYSTEM.md` §8 requires the `gmcrypto-envelope-lite` gate suite — at
current tip, in every feature configuration the crate ships, executed by its
scripted gate — to pass against every `gmcrypto-core` release candidate before
it ships. This file is that record for the **1.11.1** candidate.

**Result: PASS** — both phases, both feature configurations (`default` and
`--features aead`), 0 failures. No runtime or API incompatibility, so **no
migration note is required** before this release ships.

Unlike the 1.11.0 run (`docs/v1.11.0-gate1-evidence.md`, hand-executed under
the old 0.1.0-RC-suite rule), this run used the scripted gate that §8 now
names, so the table below is the script's own output rather than transcribed
command output. The script fails closed: a recorded run is a passing run.

## Tested commits

| Side | Commit | Notes |
|---|---|---|
| `gmcrypto-core` candidate | `f9c8ee8d997d903b92a549758de75841258aaa88` (branch `release/v1.11.1`; workspace `1.11.1`, sibling pins `=1.11.1`) | exported read-only with `git archive HEAD` |
| `gmcrypto-envelope-lite` | `0c7f98350cd2f3417dda4a3d54eda4cade603f8c` (`main`, version 0.4.0) | clean tree; exported read-only with `git archive HEAD` |

Run in a disposable export outside both checkouts. Neither repository was
modified: the pin edit and the `[patch]` block exist only in the export (the
downstream worktree was verified empty under `git status --porcelain
--untracked-files=all` after the run).

Commits on `release/v1.11.1` after `f9c8ee8` touch `docs/` only; `crates/`
and the workspace `Cargo.toml` are byte-identical between the tested candidate
and the commit that will be reviewed and tagged. The reviewer confirms this
with `git diff f9c8ee8..<reviewed-sha> --stat -- crates/ Cargo.toml` (expect
empty).

Feature configurations covered: `default` and `--features aead`. The `aead`
configuration compiles `gmcrypto-core/sm4-aead`, which pulls `gmcrypto-simd`
(and, on x86_64 and aarch64, its target-gated `cpufeatures` detection
dependency) into the graph, so it exercises core surface the default build
never reaches.

## 1. Phase 1 — pristine (committed lockfile, `--locked`)

| Configuration | Command | Result |
|---|---|---|
| shared | `cargo fmt --all -- --check` | PASS |
| shared | `sh tests/open_source_boundary.sh` | PASS |
| shared | `./ci/check-open-source-boundary.sh --worktree .` | PASS |
| default | `cargo clippy --all-targets --locked -- -D warnings` | PASS |
| default | `cargo test --all-targets --locked` | PASS (147 passed across 13 target(s)) |
| default | `cargo test --doc --locked` | PASS (5 passed across 1 target(s)) |
| aead | `cargo clippy --all-targets --locked --features aead -- -D warnings` | PASS |
| aead | `cargo test --all-targets --locked --features aead` | PASS (189 passed across 15 target(s)) |
| aead | `cargo test --doc --locked --features aead` | PASS (5 passed across 1 target(s)) |
| shared | `cargo test --locked --test release_documents` | PASS (14 passed across 1 target(s)) |
| shared | `./ci/check-crypto-inventory.sh` | PASS |

Neither pre-patch assertion was weakened. The cryptographic inventory
validates both feature-scoped tiers (`ci/crypto-inventory.snapshot` and
`ci/crypto-inventory-aead.snapshot`).

Declared downstream requirement `1.11`; candidate version `1.11.1`. The
disposable copy was pinned to `=1.11.1` so the override resolves regardless
of the declared range — the real downstream checkout is untouched. The
candidate falls inside the declared caret requirement, so no downstream
requirement bump is owed.

## 2. Phase 2 — candidate injected (disposable lockfile, no `--locked`)

| Configuration | Command | Result |
|---|---|---|
| shared | `cargo fmt --all -- --check` | PASS |
| shared | `sh tests/open_source_boundary.sh` | PASS |
| shared | `./ci/check-open-source-boundary.sh --worktree .` | PASS |
| default | `cargo clippy --all-targets -- -D warnings` | PASS |
| default | `cargo test --lib` | PASS (26 passed across 1 target(s)) |
| default | `cargo test --doc` | PASS (5 passed across 1 target(s)) |
| default | `cargo test --test aead_envelope` | PASS (0 passed across 1 target(s)) |
| default | `cargo test --test auth_and_config` | PASS (10 passed across 1 target(s)) |
| default | `cargo test --test client_convenience` | PASS (15 passed across 1 target(s)) |
| default | `cargo test --test error_redaction` | PASS (5 passed across 1 target(s)) |
| default | `cargo test --test key_roles` | PASS (15 passed across 1 target(s)) |
| default | `cargo test --test protocol_adapter` | PASS (28 passed across 1 target(s)) |
| default | `cargo test --test public_api` | PASS (1 passed across 1 target(s)) |
| default | `cargo test --test redacted_debug` | PASS (6 passed across 1 target(s)) |
| default | `cargo test --test secure_client` | PASS (12 passed across 1 target(s)) |
| default | `cargo test --test standard_vectors` | PASS (3 passed across 1 target(s)) |
| default | `cargo test --test transport_types` | PASS (12 passed across 1 target(s)) |
| aead | `cargo clippy --all-targets --features aead -- -D warnings` | PASS |
| aead | `cargo test --lib --features aead` | PASS (63 passed across 1 target(s)) |
| aead | `cargo test --doc --features aead` | PASS (5 passed across 1 target(s)) |
| aead | `cargo test --test aead_envelope --features aead` | PASS (3 passed across 1 target(s)) |
| aead | `cargo test --test auth_and_config --features aead` | PASS (10 passed across 1 target(s)) |
| aead | `cargo test --test client_convenience --features aead` | PASS (15 passed across 1 target(s)) |
| aead | `cargo test --test error_redaction --features aead` | PASS (5 passed across 1 target(s)) |
| aead | `cargo test --test key_roles --features aead` | PASS (15 passed across 1 target(s)) |
| aead | `cargo test --test protocol_adapter --features aead` | PASS (28 passed across 1 target(s)) |
| aead | `cargo test --test public_api --features aead` | PASS (1 passed across 1 target(s)) |
| aead | `cargo test --test redacted_debug --features aead` | PASS (6 passed across 1 target(s)) |
| aead | `cargo test --test secure_client --features aead` | PASS (12 passed across 1 target(s)) |
| aead | `cargo test --test standard_vectors --features aead` | PASS (5 passed across 1 target(s)) |
| aead | `cargo test --test transport_types --features aead` | PASS (12 passed across 1 target(s)) |

`release_documents` is the sole deliberate post-patch exclusion named in §8:
it validates registry metadata rather than runtime or API compatibility, and
a path package has no registry checksum. All of its assertions ran in phase 1
against the pristine lock.

`aead_envelope` reports 0 tests under `default` because it is
`#![cfg(feature = "aead")]`; it reports 3 under `aead`. That is the
configuration matrix working, not a skipped target.

## 3. Verdict

`gmcrypto-core` 1.11.1 is compatible with `gmcrypto-envelope-lite` 0.4.0
(`0c7f983`) in every configuration the envelope ships. The candidate's only
non-comment source change since `v1.11.0` sits on the
`#[cfg(feature = "sm4-bitsliced-simd")]` path (see
`docs/v1.11.1-release-review.md` §0), and no envelope configuration enables
that feature — so the envelope compiles code that is behaviour-identical to
1.11.0 in both phases.

## 4. What this gate does and does not establish

It establishes **compatibility**: the envelope builds and its entire suite
passes against the candidate, with and without `aead`.

It does **not** establish that any downstream **consumes the repair**. The
envelope's `aead` feature is `["gmcrypto-core/sm4-aead"]` only; it never
selects `sm4-bitsliced-simd`, so the repaired `tau` → `sbox_x4` composition
is not on any code path this gate compiles. The same is true of `gmcrypto-c`
(`sm4-aead` always-on, no SIMD feature). That is by design for this patch —
its audience is Rust callers who *already* opted into `sm4-bitsliced-simd`
on 1.11.0 — and it is why the patch carries no envelope or C-ABI feature
change. Whether a downstream should enable SIMD is a separate decision with
its own security-posture narrative (SIMD backends carry no constant-time
claim), recorded as a named follow-up, not a precondition of this release.

The evidence that the repaired path itself is correct lives elsewhere:
`docs/v1.11.1-release-review.md` §3 (CI on both architectures, the
million-round KAT under the feature, the SIMD fuzz profile — all landed in
#166).
